/** Enumeration for the selected art medium. */
export enum Medium {
    Animation,
    Image,
}

/** Enumeration for the selected commission tier. */
export enum Tier {
    Rendered,
    Flat,
    Lineart,
    Sketch,
}